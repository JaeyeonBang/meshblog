---
title: Code Fence Only Note
tags: [concept]
---

```typescript
// This note's body is a single code block.
// No prose, no wikilinks — just code.
function greet(name: string): string {
  return `Hello, ${name}!`
}
```
