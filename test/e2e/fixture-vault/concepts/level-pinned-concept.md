---
title: Level-Pinned Concept
tags: [concept, seed]
level_pin: 1
---

This note has `level_pin: 1` and `tags: [concept, seed]` — a common pattern for foundational concept notes that should appear at the top of graph visualisations.

The `level_pin` field overrides the PageRank-computed level assignment in `export-graph`. A value of `1` places the note at the centre of the L1 graph tier.

## Seeded concepts in meshblog

"Seed" is a tag convention for manually curated anchor nodes. These nodes anchor the graph's structure and act as starting points for new readers exploring the vault.
