---
title: Daily Review 2024-01-15
tags: [journal, daily]
---

Today I started applying the [[cross-dir-concept]] to my note-taking workflow.

## What went well

- Set up the initial vault structure with separate `concepts/` and `journal/` directories.
- Linked several concept notes from today's journal entry.
- Resolved a long-standing confusion about cross-directory wikilinks — they work as long as all directories are included in the pipeline's `baseDirs` list.

## What to improve

- The `[[plain-wikilink-target]]` note needs more depth.
- Should add an alias to `[[aliased-note]]` for the alternate way I usually refer to it.

## Links encountered

Today's note is itself a cross-directory target: it should be reachable from `concepts/cross-dir-concept.md` via `[[daily-review-2024-01-15]]`.
