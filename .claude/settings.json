{
  "$schema": "https://json.schemastore.org/claude-code-settings.json",
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Edit|Write|MultiEdit",
        "hooks": [
          {
            "type": "command",
            "command": "jq -r '.tool_input.file_path // .tool_response.filePath // empty' | { read -r f; case \"$f\" in *node_modules*) ;; */src/*|*/scripts/*) command -v graphify >/dev/null 2>&1 && graphify update . >/dev/null 2>&1 ;; esac; } 2>/dev/null || true",
            "async": true,
            "timeout": 30
          }
        ]
      }
    ]
  }
}
