---
interface Props {
  number: string;
  title: string;
  meta: string;
  href: string;
}
const { number, title, meta, href } = Astro.props;
---
<a class="note-row" href={href}>
  <span class="num">{number}</span>
  <h4>{title}</h4>
  <span class="nmeta">{meta}</span>
</a>

<style>
  .note-row {
    padding: 14px 16px 14px 0;
    border-bottom: 1px solid var(--rule-soft);
    display: grid;
    grid-template-columns: 72px 1fr auto;
    gap: 18px;
    align-items: center;
    cursor: pointer;
    transition: background var(--dur-fast) var(--ease);
    margin-right: -16px;
    padding-right: 16px;
    text-decoration: none;
    color: inherit;
  }
  .note-row:hover {
    background: var(--paper-2);
    padding-left: 16px;
    margin-left: -16px;
  }
  .note-row:focus-visible {
    outline: 2px solid var(--focus-ring);
    outline-offset: 2px;
  }
  .num {
    font-family: var(--f-mono);
    font-size: var(--fs-xs);
    color: var(--ink-3);
    letter-spacing: 0.04em;
  }
  h4 {
    font-family: var(--f-disp);
    font-size: var(--fs-lg);
    font-weight: 500;
    margin: 0;
    letter-spacing: -0.01em;
  }
  .nmeta {
    font-family: var(--f-mono);
    font-size: var(--fs-xs);
    color: var(--ink-3);
    letter-spacing: 0.04em;
  }
</style>
